import { useState } from "react";
import { DataBase } from "../firebase";
import { config } from "../firebase/config";
import { GroupedData } from "../firebase/types";
import { redirect } from "react-router-dom";

export function Admin() {
  return <div>ola</div>;
}
